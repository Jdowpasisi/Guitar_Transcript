# --- Audio & Signal Processing ---
librosa
numpy==1.26.4               # pinned — scipy, madmom, mir_eval, numba all require <2.0
soundfile
matplotlib
demucs
scipy

# --- Deep Learning (ML Core) ---
torch
torchvision
torchaudio
torchinfo
scikit-learn
jams
mir_eval
tqdm
pandas

# --- Specific Transcription Models ---
basic-pitch
crepe
# Note: madmom often requires Cython and a specific install process, 
# we will handle it when we reach P5.

# --- Backend & API ---
fastapi
uvicorn[standard]
python-multipart
celery
redis

# --- Computer Vision ---
opencv-python==4.10.0.84   # --no-deps on install; >=4.11 requires numpy>=2
mediapipe
ultralytics

# --- Utilities ---
yt-dlp
python-dotenv
