# P7 API + Worker Python dependencies
# Install inside the container (not the project venv)
# Versions pinned to match the existing GuitarAI environment

# ── API server ──────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12     # required for FastAPI UploadFile

# ── Async task queue ────────────────────────────────────────────────────────
celery[redis]==5.4.0
redis==5.1.0

# ── ML / Audio (same pins as GuitarAI venv) ─────────────────────────────────
numpy<2.0                    # 1.26.4 — madmom/mir_eval compatibility
scipy<1.12                   # 1.11.4 — madmom compatibility
librosa==0.10.2
soundfile==0.12.1
torch>=2.0.0                 # CPU build is fine; GPU auto-detected
# basic-pitch==0.4.0 is installed with --no-deps in the Dockerfile
# because it declares tensorflow<2.15.1 as a hard dep, but we only use ONNX.
# Its actual runtime deps are listed below:
pretty-midi>=0.2.9
resampy>=0.2.2,<0.4.3
demucs>=4.0.1

# ── Optional: existing project imports ──────────────────────────────────────
scikit-learn>=1.3.0
tqdm>=4.66.0
jams==0.3.5
mir_eval==0.8.2
